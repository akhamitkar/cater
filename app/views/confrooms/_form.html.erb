<%= form_for @confroom, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :conf_room, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :conf_room, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                confrooms_path, :class => 'btn' %>
  </div>
<% end %>
