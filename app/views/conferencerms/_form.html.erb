<%= form_for(@conferencerm) do |f| %>
  <% if @conferencerm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conferencerm.errors.count, "error") %> prohibited this conferencerm from being saved:</h2>

      <ul>
      <% @conferencerm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
